# apacheBeamJuliasetPy
This is basically a fork of the apache-beam juliaset example which ensures it is NOT loading the example code from the apache-beam python sdk in certain places (i.e. GCP's DataFlow) 
